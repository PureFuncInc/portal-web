import styled from '@emotion/styled'

export const MDXHeadline = styled.div(({ theme }) => `
  color: ${theme.color.text};
  text-align: center;
  font-weight: bold;
  font-size: 4em;
  margin-bottom: 25px;
`)
